spring:
  application:
    name: config-server
  boot:
    admin:
      client:
        url: http://localhost:8760
  jmx:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"  
  endpoint:
    health:
      show-details: ALWAYS
