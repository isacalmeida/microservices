server:
  port: 8100

spring:
  application:
    name: acesso-service
  cloud:
    config:
      uri: http://localhost:8888
  jmx:
    enabled: true
  jackson:
    serialization:
      INDENT_OUTPUT: true

security:
  jwt:
    public-key: classpath:public.txt
