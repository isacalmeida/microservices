spring:
  application:
    name: pessoa-service
  cloud:
    consul:
      config:
        format: YAML
      discovery:
        instanceZone: zone1
        instance-id: "${spring.cloud.client.hostname}:${spring.application.name}:${random.int[1,999999]}"
      host: http://ec2-34-201-164-172.compute-1.amazonaws.com
      port: 8500
  profiles:
    active: desenv,zone1