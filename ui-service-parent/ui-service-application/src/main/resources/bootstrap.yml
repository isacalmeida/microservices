server:
  port: 9000

spring:
  application:
    name: ui-service
  cloud:
    config:
      uri: http://localhost:8888
  main:
    allow-bean-definition-overriding: true
  
logging:
  level:
    root: INFO
